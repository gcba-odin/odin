'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

exports.default = function (type, config) {
  if (hash[type.toLowerCase()] instanceof Function) {
    return new hash[type.toLowerCase()](config);
  } else {
    throw new Error('Unrecognized type -> ' + type);
  }
};

var _BCryptHash = require('./BCryptHash');

var _BCryptHash2 = _interopRequireDefault(_BCryptHash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var hash = {
  bcrypt: _BCryptHash2.default
};

/**
 * Create Hash instance
 * @param {String} type Type of hash instance
 * @param {Object} [config] Additional options for hash instance
 * @returns {*}
 */
module.exports = exports['default'];