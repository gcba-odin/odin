"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Category');
const Fixture = require('../../fixtures/CategoryFixture');

describe('models:Category', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
