"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/FileType');
const Fixture = require('../../fixtures/FileTypeFixture');

describe('models:FileType', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
