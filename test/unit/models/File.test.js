"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/File');
const Fixture = require('../../fixtures/FileFixture');

describe('models:File', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
