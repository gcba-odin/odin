"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Organization');
const Fixture = require('../../fixtures/OrganizationFixture');

describe('models:Organization', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
