"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/HeadController');

describe('controllers:HeadController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
