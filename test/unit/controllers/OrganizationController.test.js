"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/OrganizationController');

describe('controllers:OrganizationController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
