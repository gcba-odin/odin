"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/OptionsController');

describe('controllers:OptionsController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
