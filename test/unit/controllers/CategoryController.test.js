"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/CategoryController');

describe('controllers:CategoryController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
