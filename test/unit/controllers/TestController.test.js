"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/TestController');

describe('controllers:TestController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
