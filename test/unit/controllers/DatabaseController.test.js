"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/DatabaseController');

describe('controllers:DatabaseController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
