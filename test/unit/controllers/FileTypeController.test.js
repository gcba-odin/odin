"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/FileTypeController');

describe('controllers:FileTypeController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
